import numpy as np
import matplotlib.pyplot as plt
from scipy.stats import pearsonr, norm

x = np.random.normal(0,1,100)
y = x + np.random.normal(0,1,100)

print("Correlation:", pearsonr(x,y)[0])

plt.scatter(x,y)
plt.title("Scatter Plot")
plt.show()


Output:
Correlation: ~0.70 (approx)
